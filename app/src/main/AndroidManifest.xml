<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sanardev.instagrammqtt">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".core.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true"
        android:debuggable="true"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning,HardcodedDebugMode"
        tools:replace="android:label">
        <activity android:name=".ui.main.MainActivity">

        </activity>
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.twofactor.TwoFactorActivity">

        </activity>
        <activity android:name=".ui.direct.DirectActivity">

        </activity>

        <service android:name=".mqtt.service.NettyService">
            <intent-filter>
                <action android:name="com.sanardev.instagrammqtt.ACTION_CHECK_SESSION" />
                <action android:name="com.sanardev.instagrammqtt.ACTION_HEARTBEAT" />
                <action android:name="com.sanardev.instagrammqtt.ACTION_CONNECT_SESSION" />
                <action android:name="com.sanardev.instagrammqtt.ACTION_DISCONNECT_SESSION" />
            </intent-filter>
        </service>

    </application>

</manifest>